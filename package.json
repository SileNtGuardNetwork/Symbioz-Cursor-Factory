{
  "name": "symbioz-cursor-factory",
  "version": "0.1.0-alpha.0",
  "private": true,
  "description": "Open-source Cursor operating system for structured SaaS product development.",
  "license": "MIT",
  "scripts": {
    "validate": "node scripts/validate-foundation.mjs",
    "doctor": "node scripts/doctor.mjs",
    "verify": "npm run validate && npm run doctor",
    "test": "npm run verify"
  },
  "engines": {
    "node": ">=20"
  }
}
